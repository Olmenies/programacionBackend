div(class='divFormContainer')
    h2='Ingresar un producto'
    form(action='/api/products',method='POST', id='productsForm')
        label(for='title')='Título'
        input(type='text', name='title', id='formTitleInput' required)
        br

        label(for='price')='Precio' 
        input(type='number', name='price', id='formPriceInput' required)
        br

        label(for='image')='Imagen URL' 
        input(type='string', name='image', id='formImageInput' required)
        br

        input(type='submit', value='Enviar', id='formSubmitButton')